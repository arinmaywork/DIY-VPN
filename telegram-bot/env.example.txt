# Required
TG_BOT_TOKEN=          # from @BotFather
TG_ALLOWED_USERS=      # comma-separated Telegram user IDs (e.g. "123456,789012")

# One of these is required (VPN_BOXES preferred):
#
# Multi-box: csv of "name:ip" pairs. Names are arbitrary labels you'll see
# in /boxes and pass to /switch.
VPN_BOXES=             # e.g. "toronto:40.233.120.150,london:1.2.3.4"
#
# Single-box fallback (legacy): treats the box as one named "default".
# VPN_HOST=40.233.120.150

# Optional (defaults shown). These apply to every box.
# VPN_SSH_USER=ubuntu
# VPN_SSH_KEY_PATH=~/.ssh/diyvpn-oracle
# VPN_SSH_PORT=22
# VPN_SSH_KNOWN_HOSTS=~/.ssh/known_hosts
# TG_LOGLEVEL=INFO

# Optional: where to persist the active-box selection between bot restarts.
# Defaults to .bot-state.json in the working directory.
# DIYVPN_BOT_STATE=/var/lib/diyvpn-bot/state.json
